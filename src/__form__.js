/**
 * 1. use from tag and onSubmit handler with event (e)
 * Form e access e.target.[name of the input field].value
 *
 * 2. controlled element: use individual field state for each input field
 *
 * 3.controlled element: one object in a state holding all the input fields value
 *
 * 4.uncontrolled element: useRef to creat a reference to thye element and access value by using like: nameRef.current.value
 *
 */
